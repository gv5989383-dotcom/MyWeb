import random

def analyze_video_tool(video_path: str):
    # Placeholder for real ML model
    result = random.choice(["FAKE", "REAL"])
    confidence = round(random.uniform(0.7, 0.99), 2)
    return result, confidence
